import { Card } from "@/components/ui/card"

const AppointmentsRequests = () => {
    return (
        <Card className="flex flex-col gap-4 p-6 w-full h-full">AppointmentsRequests</Card>
    )
}

export default AppointmentsRequests